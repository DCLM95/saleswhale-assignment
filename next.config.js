/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    domains: ["d1bb37ap2qun5z.cloudfront.net"],
  },
};

module.exports = nextConfig;
